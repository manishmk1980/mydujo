{
  "name": "mdpl-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test:api": "node tests/api-smoke.mjs",
    "data:sanitize": "node scripts/sanitize-legacy-data.mjs",
    "data:sanitize:dry-run": "node scripts/sanitize-legacy-data.mjs --dry-run",
    "prisma:generate": "prisma generate",
    "prisma:migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "7.4.2",
    "@prisma/client": "7.4.2",
    "argon2": "^0.44.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "17.3.1",
    "express": "4.22.1",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.10"
  },
  "devDependencies": {
    "prisma": "7.4.2"
  }
}
